{
  "name": "@afterimage/studio-contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b tsconfig.json --force",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "node -e \"console.log('lint placeholder: @afterimage/studio-contracts')\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@afterimage/export-profiles": "workspace:*",
    "@afterimage/project-model": "workspace:*"
  }
}
