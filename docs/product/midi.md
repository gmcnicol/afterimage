# MIDI model

MIDI is a shared abstraction with two runtime uses:

- Studio: authoring and recorded automation
- Live: performance input and reactive control

The mapping schema must remain runtime-neutral.
