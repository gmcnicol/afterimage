# Output model

The system supports three broad output classes:

- rendered files
- live projector/fullscreen output
- network streaming / local recording

The runtime decides which outputs are active; the project model stays stable.
