{
  "name": "@afterimage/live-desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:renderer": "vite",
    "dev": "concurrently -k \"pnpm dev:renderer\" \"wait-on tcp:5174 && electron ./electron/main.js\"",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "node -e \"console.log('lint placeholder: @afterimage/live-desktop')\"",
    "test": "node -e \"console.log('test placeholder: @afterimage/live-desktop')\""
  }
}
